services:
  amtgardidpapp:
    build:
      context: .
      dockerfile: docker/Dockerfile.prod
    working_dir: /var/www
    volumes:
      - .:/var/www/idp.amtgard.com
    env_file:
      - .prod.env
      - .env
    environment:
      - ENVIRONMENT=PROD
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - amtgard-idp-net

networks:
  amtgard-idp-net:
